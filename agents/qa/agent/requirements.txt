# Deployment zip is Lambda-style: these are vendored into the artifact.
#
# playwright is the CLIENT library only. The browser itself is AgentCore's
# remote Chromium, reached over CDP, so `playwright install` is never run and no
# browser binaries ship in the zip.
bedrock-agentcore>=1.22.0
playwright>=1.49.0

# boto3 is intentionally NOT pinned here -- the managed runtime provides it, and
# vendoring a second copy only risks a version skew against the runtime's.
