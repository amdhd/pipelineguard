# Runtime deps for the cost gate Lambda.
# boto3 is provided by the Lambda Python 3.12 runtime — pinned here for local dev only.
boto3>=1.34.0

# Test-only (installed locally, not packaged into the Lambda zip):
pytest>=8.0.0
